// Concatenating with String.protoype.concat() and using "+" to concatenate

const firstName = "Ben";
const lastName = "Lane";

console.log(firstName + " " + lastName);
console.log(firstName.concat(" ", lastName));
