// Write a function evenOrOdd that determines whether the argument is an even or odd number, and then logs either 'even' or 'odd' to the console respectively

const evenOrOdd = (num) => {
	if (num % 2 === 0) {
		console.log('even');
	} else if (num % 2 === 1) {
		console.log('odd');
	}
}
  
