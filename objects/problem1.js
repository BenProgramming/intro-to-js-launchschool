// Given the following code, how can you access the name of the person?  

let person = {
  name:       'Bob',
  occupation: 'web developer',
  hobbies:    'painting',
};

// by using person.name or by using person['name'];  
console.log(person.name);
console.log(person['name']);
